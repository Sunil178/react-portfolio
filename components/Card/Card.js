import styles from './Card.module.css'
import ArrowForwardIcon from '@material-ui/icons/ArrowForward';

export default function Card ({name, percentage}) {
    return (
        <>
            <div className={styles.card_container}>Hello</div>
        </>
    )
}
